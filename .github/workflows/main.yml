name: Hello Workflow
on: push
jobs:
  build:
    name: Hello Workflow Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: ./.github/actions/hello
        with:
          MY_NAME: "Bob"